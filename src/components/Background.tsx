import React from "react";

const Background = () => {
  return (
    <div
      className="mesh-gradient-bg fixed inset-0 pointer-events-none"
      aria-hidden="true"
    />
  );
};

export default Background;
